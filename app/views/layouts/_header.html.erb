<%= link_to 'Sign Up', signup_path unless user_signed_in? %>
<%= link_to 'Log In', login_path unless user_signed_in? %>
<%= link_to 'Log Out', logout_path if user_signed_in? %>
<%= link_to 'New Post', new_post_path if user_signed_in? %>
<%= "Hello, #{current_user.name}!" if user_signed_in? %>
